- switch:
    - name: "Main Server"
      unique_id: main_server_power

      state: >
        {{ is_state(
          'binary_sensor.main_server',
          'on'
        ) }}

      turn_on:
        - action: rest_command.wake_mainserver

      turn_off:
        - action: shell_command.poweroff_mainserver
